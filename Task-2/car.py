class Car:
        def __init__(self, make, model, mileage, price, feats):
                self.make = make
                self.model = model
                self.mileage = mileage
                self.price = price
                self.feats = feats
